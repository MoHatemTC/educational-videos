# ─────────────────────────────────────────────────────────────────────────────
# Vision-Based UI Agent — Python dependencies
# Python 3.11 required
# ─────────────────────────────────────────────────────────────────────────────

# Browser automation
browser-use==0.1.40           # browser-use 0.12-series; CDP backend for Playwright
playwright>=1.44,<2.0         # Playwright async API (Chromium)

# HTTP client — used for direct Gemini REST calls (async)
httpx>=0.27,<1.0

# Vision / image processing
Pillow>=10.0,<12.0            # Screenshot handling & GIF compilation

# Configuration
python-dotenv>=1.0,<2.0       # .env loading

# Pretty terminal output
rich>=13.0,<14.0
